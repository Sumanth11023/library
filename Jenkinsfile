@Library('library') _
pipeline {
    agent any

    stages {
        stage('greeting') {
        //     steps {
        //         greet ("Raja")
        //         greet ("Mahi")
        //     }
        // }
        stage('greeting') {
            steps {
                add(10,20)
            }
        }
        
    }
}
